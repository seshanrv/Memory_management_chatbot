{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "g++ build active file",
            "type": "shell",
            "command": "rm -rf build && mkdir build && cd ./build && cmake -DCMAKE_BUILD_TYPE=Debug .. && make"
        }
    ]
}